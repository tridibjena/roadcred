# --- core DL ---
torch>=2.8.0
torchvision>=0.23.0

# --- segmentation models ---
segmentation-models-pytorch>=0.4.0  # DeepLabV3+ / U-Net / FPN decoders
timm>=1.0.0                       # encoder zoo backing smp

# --- data / augmentation ---
albumentations>=1.4.0
opencv-python-headless>=4.10.0
pillow>=10.0.0
numpy>=1.26,<3
pandas>=2.0.0
pyyaml>=6.0

# --- experiments / analysis ---
matplotlib>=3.8.0                 # every figure under results/figures/

# --- compression / export ---
onnx>=1.16.0
onnxruntime>=1.19.0               # INT8 PTQ + CPU latency benchmarking
onnxscript>=0.1.0                 # required by torch.onnx.export (dynamo path)

# --- serving ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9           # multipart image upload for /predict

# --- logging ---
tensorboard>=2.17.0

# --- tests ---
pytest>=8.0.0
ruff>=0.6.0
