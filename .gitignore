# --- data (never commit; see DOWNLOADS.md) ---
data/raw/
data/processed/
!data/raw/*/PUT_FILES_HERE.md

# --- model weights & training artifacts ---
checkpoints/
runs/
*.pt
*.pth
*.onnx
# torch.onnx.export writes weights to a UUID-named sidecar in the cwd; compression.export
# inlines them back into the .onnx, but the exporter's own temporaries can outlive the run.
*.onnx.data
*.data
sym_shape_infer_temp.onnx

# --- python ---
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.ipynb_checkpoints/
*.egg-info/

# --- optuna / experiment stores ---
*.db

# --- os / editor ---
.DS_Store
.vscode/
.idea/
